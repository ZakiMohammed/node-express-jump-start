const person = {
    firstName: 'John',  // key: value (number, string, boolean, object)
    lastName: 'Doe'
};

console.log(person);
console.log(person.firstName)

person.firstName = 'Allen';

console.log(person.firstName);